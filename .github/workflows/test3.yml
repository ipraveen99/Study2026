name: Website CI
 
on:
  push:
    branches:
    - main
    
jobs:
  build:
    runs-on: ubuntu-latest
 
    steps:
    - name: Checkout
      uses: actions/checkout@v4
 
    - name: Display ls
      run: ls 
      
  verify:
# needs build
    runs-on: ubuntu-latest
 
    steps:
    - name: Finished
      run: echo Finished
 
