name: Website CI
env:
  APP_NAME: devops-company-site
  ENVIRONMENT: development
  COMPANY: CloudNova
 
on:
  push:
    branches:
    - main
    
jobs:
  build:
    runs-on: ubuntu-latest
 
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Application Name ${{env.APP_NAME}}
      run: |
        echo "Verify the following details"
        echo $APP_NAME
        echo $ENVIRONMENT
        echo $COMPANY
      env:
        APP_NAME: Test App
        ENVIRONMENT: Test Environment
 
    - name: Display ls for ${{env.COMPANY}}.${{env.ENVIRONMENT}}.${{env.APP_NAME}}
      run: ls 
      
  verify:
    needs: build
    runs-on: ubuntu-latest
 
    steps:
    - name: Finished
      run: echo Finished
