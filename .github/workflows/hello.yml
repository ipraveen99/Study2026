name: Practice

on:
  push:
    branches:
    - main

jobs:
  Lab-1-First-Workflow:
    runs-on: ubuntu-latest
    steps:
      - name: "Lab 1 : First Workflow"
        run: echo 'Hello DevOps'

  Lab-2-Linux-Runner:
    needs: Lab-1-First-Workflow
    runs-on: ubuntu-latest
    steps:
      - name: "Lab 2 : Linux Runner"
        run: |
            echo "  Present Working Directory : " pwd
            echo "  Who am i : " whoami
            echo "  Date : " date
            echo "  User : " user -a

  Lab-3-Repository-Inspection:
    needs: Lab-2-Linux-Runner
    runs-on: ubuntu-latest
    steps:
      - name: "Lab 3 : Repository Inspection"
        run: |
            ls
            ls -a
            find

  Lab-4-Environment-Variables:
    needs: Lab-3-Repository-Inspection
    runs-on: ubuntu-latest
    steps:
      - name: "Lab 4 : Environment Variables"
        run: |
            echo "GITHUB_WORKFLOW : " ${{github.workflow}}
            echo "GITHUB_REPOSITORY : " ${{github.repository}}
            echo "GITHUB_REF : " ${{github.ref}}
            echo "GITHUB_SHA : " ${{github.sha}}
            echo "RUNNER_OS : " ${{runner.os}}
          
  Lab-5-Multiple-Steps-System-Info:
     needs: Lab-4-Environment-Variables
     run-on: ubuntu-latest
     steps:
       - name: "Lab 5 : Multiple Steps System Info"
         run: |
             echo "  Present Working Directory : " pwd
             echo "  Who am i : " whoami
             echo "  Date : " date
             echo "  User : " user -a
             
