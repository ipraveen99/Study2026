# This is a workflow to test the github actions
name: Test Workflow

on: 
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Fetch repository code
        uses: actions/checkout@v7

      - name: Run some command
        run: echo "Hellow world"
